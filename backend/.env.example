PORT=3000
MONGODB_URI=mongodb://localhost:27017/ikaros
JWT_SECRET=change-this-secret-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=change-this-different-secret-in-production
JWT_REFRESH_TTL_DAYS=30
FRONTEND_URL=http://localhost:5173

# SMTP (transakční maily). Když nejsou SMTP_HOST + SMTP_USER nastaveny,
# mailer jen loguje (dev). Gmail: SMTP_PASS = App Password (ne běžné heslo).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Projekt Ikaros <tyky.projekt.ikaros@gmail.com>
